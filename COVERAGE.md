# Test Coverage Report

Generated by running the test suites locally. See below for how to
regenerate these yourself.

## Backend (Go)

```
ok  	calculator-backend	         	coverage: 61.0% of statements
ok  	calculator-backend/calculator	coverage: 100.0% of statements

calculator/calculator.go   — 100% (all arithmetic operations + dispatch)
handlers.go                — 75–100% per function (calculateHandler: 93.8%)
main.go                    — 0% (server bootstrap / CORS wrapper — see note)

TOTAL: 72.6% of statements
```

The `calculator` package — the actual arithmetic logic — is **100% covered**.
The uncovered lines live in `main.go` (`main()` and the `withCORS` wrapper),
which start a real HTTP server and aren't meaningfully unit-testable without
spinning up the whole process; they're implicitly exercised by every request
in `handlers_test.go` going through `calculateHandler`/`healthHandler`
directly instead.

**HTML report:** [`backend/coverage.html`](./backend/coverage.html) — open
in a browser for a line-by-line view.

**Regenerate:**
```bash
cd backend
go test ./... -coverprofile=coverage.out
go tool cover -html=coverage.out -o coverage.html
```

## Frontend (React/TypeScript)

```
All files            86.66% Stmts | 83.54% Branch | 80.43% Funcs | 90.83% Lines

api/                 100%   (calculatorClient.ts, types.ts)
hooks/               89.28% (useCalculator.ts — the state machine)
utils/               100%   (formatNumber.ts)
components/          66.66% (see note below)
```

`api/`, `hooks/`, and `utils/` — where the actual logic lives — are all
89–100% covered. `components/Calculator.tsx` shows lower coverage because
the coverage tool counts every individual button's JSX as a separate
branch; the component *is* exercised end-to-end by
`Calculator.test.tsx` (clicking through a full `6 × 7 = 42` flow, error
states, and Clear), it's just that with 19 buttons on screen, only a
handful get clicked per test. The `.css` files show 0% because CSS isn't
executable code — vitest lists them as files but they're not part of
coverage in any meaningful sense.

**HTML report:** [`frontend/coverage/index.html`](./frontend/coverage/index.html)
— open in a browser for a line-by-line view.

**Regenerate:**
```bash
cd frontend
npm run test:coverage
```
